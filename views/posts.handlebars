<div class="center-title">
	<p>
		Post
	</p>
</div>
<div class="post-card">
	<div class="card-label">
		<span class="card-title"><a
				href="dashboard/edit-post/{{this.id}}"
				id="postPath"
			>{{this.post_title}}</a></span>
		<span class="card-info"><div>post ref#
				<span id="postRef">{{this.id}}</span></div>{{this.user.username}}
			{{this.post_date}}</span>
	</div>
	<div class="card-body">
		<p>
			{{this.post_body}}
		</p>

		<span class="deletePost"><button>delete</button></span>
	</div>
</div>

<div class="center-title">
	<p>
		Create comment
	</p>
</div>

<div class="post-form">
	<form>
		<ul>
			<li>
				<label for="comment_body">comment:</label>
				<input type="text" id="comment_body" name="comment" />
			</li>
		</ul>
		<button type="submit" id="new-comment">Submit</button>
	</form>
</div>

<div class="center-title">
	<p>
		Comments
	</p>
</div>
{{#if comments}}
	{{#each comments}}
		<div class="post-card">
			<div class="card-label">
				<span class="card-title">{{this.user.username}}</span>
				<span class="card-info">{{this.comment_date}}</span>
			</div>
			<div class="card-body">
				<p>
					{{this.comment_body}}
				</p>
			</div>
		</div>
	{{/each}}
{{/if}}
<script src="/js/comment.js"></script>